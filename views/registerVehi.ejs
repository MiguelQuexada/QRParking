<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Register Horus</title>
    <link rel="icon" href="resources/images/logo.png">
    <link rel="stylesheet" href="resources/styles/register.css">    
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" >   
</head>
    <body>
        <% if(login){ %>
        <form action="registerVehi" method="post">
            <div class="form">
                <h1>REGISTRO<br>VEHICULO</h1>
                <div class="grupo">
                    <input type="text" name="placa" id="placa" required><span class="barra"></span>
                    <label for="">Placa</label>
                </div>
                <div class="grupo">
                    <input type="text" name="tipoVehiculo" id="tipoVehiculo" required><span class="barra"></span>
                    <label for="">Tipo de Vehiculo</label>
                </div>  
                <div class="grupo">
                    <input type="text" name="bahia" id="bahia" required><span class="barra"></span>
                    <label for="">Bahia</label>
                </div>                     
                <div class="grupo">
                    <input type="text" name="idUsuario" id="idUsuario" required><span class="barra"></span>
                    <label for="">ID del usuario</label>
                </div>                      
                <button type="submit">Registrar</button>     
                <a href="homeAdmin"><button class="salir" type="button">Volver</button></a>                           
            </div>                   
        </form>
        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
        <% if (typeof alert != "undefined"){%>
            <script>
                Swal.fire({
                    title:'<%= alertTitle %>',
                    text: '<%=alertMessage %>',
                    icon: '<%=alertIcon %>',
                    showConfirmButton: <%= showConfirmButton %>,
                    timer: <%= timer %>
                }).then(()=>{
                    window.location='/<%=ruta%>'
                })
            </script>
            <% } %>        



        <%} else {%>
            <div class="centro">
                <div class="grupo">
                    <h1><strong><%=nombre%></strong></h1>
                </div>
                <a href="/"><button type="logout">Ingresar</button></a>  
            </div>
        <% } %>   
 

    </body>
</html>